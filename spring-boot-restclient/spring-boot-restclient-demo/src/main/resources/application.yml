# spring-boot-restclient-demo 演示模块配置
# 演示 RestClient 对外发起 HTTP 请求的最佳实践

# Spring 框架配置
spring:
    # 应用名称，用于服务注册与日志追踪
    application:
        name: "spring-boot-restclient-demo"

# 日志配置
logging:
    level:
        # Logbook HTTP 请求日志输出 TRACE 级别
        org.zalando.logbook: TRACE
