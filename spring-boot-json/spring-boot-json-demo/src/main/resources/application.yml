# spring-boot-json-demo 演示模块配置
# JSON 处理演示模块的最小运行配置

# 服务器配置
server:
    # 监听端口
    port: 8087

# Spring 框架配置
spring:
    # 应用名称
    application:
        # 服务名称，用于服务注册与日志追踪
        name: "spring-boot-json-demo"
